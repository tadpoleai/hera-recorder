const defaultHost = window.location.hostname;
//defaultHost = "192.168.1.49";
const defaultPort = 9090;
const defaultHttps = false;
const defaultPath = '';
const syncPeriodMs = 5000;
const dataPeriodMs = 1000;

export default {
  defaultHost,
  defaultPort,
  defaultHttps,
  defaultPath,
  syncPeriodMs,
  dataPeriodMs
};
